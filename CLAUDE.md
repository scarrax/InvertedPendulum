# CLAUDE.md

Stable project context for Claude Code sessions in this repo: what's done, what's in progress, and the conventions that must hold across all work packages (AP1–AP6). This file is the durable memory of the *project*, not of any one conversation — session resume/context handles "what are we doing right now"; this file answers "what is true about the project regardless of which session is running."

Maintenance rule: when a work package (AP) reaches a new status, update its row in the table below and append a dated entry under "AP History" — don't rewrite history, append to it. Keep entries factual and terse (what changed, not how). Do not record in-progress task-level detail here (which task of which plan is mid-review, ledger state, etc.) — that lives in `.superpowers/sdd/<plan>/progress.md` inside the relevant worktree and is expected to disappear once a branch finishes.

## Project Overview

Inverted pendulum: an OpenModelica model (cart + pendulum) exported as an FMU, driven in real time by a Pygame game (`pendulum_game_controlled.py`) via `fmpy`. Full scope and Arbeitspakete are defined in `Projektanweisung_Invertiertes_Pendel.md`.

## Arbeitspakete (AP) Status

| AP | Description | Status |
|----|---|---|
| AP1 | Modellierung mit Standardbibliotheken (MultiBody FMU vs. formelbasiertes Modell) | ✅ Done — validated, see `AP1_Validierung.md` |
| AP2 | Interaktive Steuerung und Punktesystem | ✅ Done — merged to `main`, human-verified (H/P/R, MultiBody FMU) |
| AP3 | Automatisierte Regler (LQR, Swing-up, …) | ✅ Done — Teil 1 (LQR), Teil 2 (Swing-up), Teil 3 (Reglervergleich) alle merged zu `main`, siehe `AP3_Reglervergleich.md` |
| AP4 | Schwierigkeitsgrade | ✅ Done — merged to `main`, human-verified (D cycles Leicht/Standard/Schwer, all 3 levels feel differentiated) |
| AP5 | Schriftliche Ausarbeitung | ⬜ Not started |
| AP6 | Präsentation | ⬜ Not started |

### AP History

- **2026-08-03**: AP1 validated — MultiBody model matches the flat reference model (momentary acceleration comparison, not trajectory comparison — see convention below). Euler solver required on Windows; CVODE crashes with `STATUS_HEAP_CORRUPTION`.
- **2026-08-05**: AP2 done and merged to `main` (scoring stability bonus, Auto/Manual/Mixed mode tracking, leaderboard `Mode`/`Difficulty` columns + legacy-CSV backfill fix, MultiBody FMU switch, pause `P`, reset `R` with corrected FMI2 `setupExperiment()` sequence). All 6 tasks passed task-scoped review (two needed a fix round); final whole-branch review found and fixed a cross-task gap (reset didn't clear `auto_time`/`manual_time`) plus tooling/robustness issues (root `conftest.py`, leaderboard NaN handling). Human-verified interactively (H/P/R, MultiBody FMU) after merge. Known, deliberately deferred: the stability bonus makes new scores ~40x pre-AP2 leaderboard scores with no visual distinction — noted, not fixed.
- **2026-08-06**: AP3 Teil 1 (LQR-Regler) done and merged to `main`. `LQRController` linearized around `φ=π` using the *corrected, coupled* equations of motion from `AP1_Validierung.md` §6 (not the flat `pendulum.mo` model), gain computed at runtime via `scipy.linalg.solve_continuous_are`. New `L` key toggles between `SimpleController` ("PD") and `LQR` in Auto mode, independent of `H`. All 3 tasks passed task-scoped review; final whole-branch review independently re-derived the linearization by hand *and* ran the closed-loop control law headless against the real `InvertedPendulumMB.fmu`, confirming LQR balances/recenters from up to ~11° deviation vs. SimpleController's ~2° — then found and fixed a badge-overflow rendering bug and a missing regression test that would've let the linearization silently regress to the (wrong) flat-model coefficients. Human-verified interactively (`L`, LQR holds the pendulum up). New `requirements.txt` (first in the project; `numpy`/`scipy` are new dependencies). Swing-up controller and the formal multi-controller comparison are separate, later AP3 slices — not started.
- **2026-08-13**: AP3 Teil 2 (Swing-up-Regler) done and merged to `main`. `SwingUpController` composes an internal `LQRController` and switches to it via hysteresis (`CAPTURE_THETA=10°`/`CAPTURE_VPHI=2.0` → `RELEASE_THETA=25°`); energy-pump law and its `τ`-inversion are exact, derived from the same coupled equations as the LQR (`AP1_Validierung.md` §6.2), no approximation. Both tasks passed task-scoped review (Task 2 needed 2 fix rounds for a badge-overflow regression, same class of bug as AP3 Teil 1's — labels abbreviated to `SU:s`/`SU:b`). Final whole-branch review found the swing-up **infeasible** against the real FMU at the original `d_pend=0.15`/`MAX_TAU=10` (max energy 1.59J vs. 4.9J needed, confirmed via an ideal bang-bang pump hitting the identical ceiling — a plant limitation, not a tuning bug, foreseen in `AP1_Validierung.md` §6.6). Fixed by lowering `InvertedPendulumMB.mo`'s `d_pend` to `0.01` (re-exported via OMEdit) and retuning `K_ENERGY` to `10` — found via direct numerical simulation of the coupled ODEs before spending an OMEdit round-trip, then confirmed end-to-end against the rebuilt FMU (captures in ~3-4s). Human interactive testing then caught a second, independent bug this exposed: **explicit-Euler numerical instability** — at the lower damping, single `0.02s` FMU co-simulation steps artificially inject energy, making pure Manual mode (zero force input) swing the pendulum up on its own, which is physically impossible under dissipation-only dynamics. Fixed by sub-stepping the FMU call 10x per game frame (`SUBSTEPS=10`, `tau` still set once per outer frame — no change to timing/scoring/actuation resolution); added an FMU-driven regression test (`tests/test_numerical_stability.py`, skips gracefully without the FMU present) so a future revert can't reintroduce this silently. Both fixes independently re-verified against the real FMU (not just unit tests) before and after review. Known, deliberately accepted tradeoff: manual-mode idle now takes ~180-200s to settle instead of ~8s (still correctly decaying, not a bug) — documented in `AP1_Validierung.md` §6.7, accepted in favor of fast/robust swing-up capture. Formal multi-controller comparison remains a separate, later AP3 slice — not started.
- **2026-08-17**: AP3 Teil 3 (formaler Reglervergleich) done and merged to `main`. New standalone `benchmark_controllers.py` (headless, no Pygame) drives the three existing controllers (unmodified) against the real FMU and measures stability (capture-envelope sweep), reaction time (settling time at fixed baselines), and robustness (recovery time + peak deviation after a force pulse on `tau`), plus a swing-up-specific capture-time scenario from the real game start condition; writes `AP3_Reglervergleich.md` (committed) and PNG plots to `benchmark_plots/` (gitignored, regenerable). All 5 tasks passed task-scoped review. A design-time spike found the originally planned disturbance mechanism (a direct `vphi` state kick) technically impossible against the real Co-Simulation FMU (`phi`/`vphi` are `causality=output, initial=calculated`, not externally settable, verified empirically) — switched to a force pulse on `tau` (the only genuine runtime input) before implementation started. Mid-implementation, Task 2 found and an eigenvalue analysis of the closed-loop `A − B·K` matrix independently confirmed that **`SimpleController` ("PD")'s closed loop is linearly unstable near the upright position** (positive real-part eigenvalue at both the historical `d_pend=0.15` and current `d_pend=0.01`) — not a new regression from AP3 Teil 2, and not a bug in this benchmark; PD only satisfies the benchmark's 5°/1s hold criterion for very small initial deviations (empirically ~1.5°) because divergence takes longer than the hold window to cross the tolerance boundary. This means PD's real capture margin sits *below* the sweep's first tested point (2°), so PD correctly reports "kein Erfolg im gesweepten Bereich" rather than a small positive number — a genuine, previously undocumented finding about the existing PD controller, not a benchmark defect. Final whole-branch review found the robustness metric itself was under-dimensioned (the spec's fixed `KICK_TAU=8.0`/`KICK_STEPS=5` pulse only deflects the pendulum ~1.0° against LQR/SwingUp, well inside the 5° tolerance, making `recovery_time=0.00s` an artifact of the disturbance being too weak to register rather than fast control) — fixed in one review-mandated fix wave by adding a `peak_post_kick_deviation_deg` field/report column (not by changing the disturbance magnitude, which the spec explicitly rules out as a scope decision) and rewriting the report's Diskussion section to name the PD eigenvalue finding explicitly instead of a softer "reacts only locally" phrasing. Real measured results (see `AP3_Reglervergleich.md` for the full report): PD envelope below 2° (no capture in the swept range), LQR envelope 10°, SwingUp captures the real ~-112.5°-from-upright start condition in 4.20s.
- **2026-08-19**: AP4 (Schwierigkeitsgrade) done and merged to `main`. Three difficulty levels (Leicht/Standard/Schwer) vary the scoring tolerance zones (`bonus_zone`/`tight_bonus_zone`, now parameters of `compute_score_increment()`) and the FMU's mass/friction parameters (`m_cart`/`m_pend`/`d_cart`/`d_pend` — same `causality=parameter` family as `phi0`/`vphi0`, settable via `setReal()` before `enterInitializationMode()`), active only in Manual mode via a new `D` key (cycles Leicht→Standard→Schwer→Leicht, triggers an implicit reset). A mutual-exclusion invariant guarantees Auto mode never runs with non-Standard physics: `H` is a no-op unless `difficulty == "Standard"`, `D` is a no-op while `auto_mode` is `True` — so AP3's controllers (tuned to the original physics) never needed retuning. `Standard` reproduces the pre-AP4 AP3 values bit-for-bit (confirmed against the real FMU: identical trajectories). All 5 tasks passed task-scoped review on the first pass. Final whole-branch review (opus) independently re-derived the mutual-exclusion safety property from the merged source (confirmed unreachable-forbidden-state), proved `reset_round()` actually reaches the solver (not just the getter, via a mid-run reset trajectory test), measured every badge/hint label against real `pygame.font` metrics, and confirmed all 3 levels stay energy-bounded under the Euler/`SUBSTEPS=10` scheme — found 2 Important issues, fixed in one bundled fix wave with a clean scoped re-review: (1) the on-screen leaderboard didn't show which difficulty a score was earned at, despite up to ~11× score spread between levels at equal play quality (fixed: display-only, added `Difficulty` to the rendered line, CSV/sorting untouched); (2) the Euler energy-injection regression guard (`tests/test_numerical_stability.py`, from AP3 Teil 2) only covered `Standard` physics, not the two new configs this branch adds (fixed: parametrized over all 3 levels). Post-review **human interactive playtest found the original `Leicht` values backwards**: raising `d_cart`/`d_pend` above `Standard` (intended to make holding easier) instead fought the mandatory manual swing-up phase — the round starts ~112° from upright, outside the ≥90° scoring cone, so extra damping ate the energy a player pumps in via bang-bang `tau` before any points could be earned at all, producing a real measured score of **0.0** (Leicht was harder than Schwer). Fixed by lowering `Leicht`'s damping *below* `Standard` instead of above (`d_cart` 0.30→0.08, `d_pend` 0.05→0.01); verified headless by driving the real FMU with the existing `SwingUpController` energy-pump law used purely as a physical feasibility oracle (not as an Auto-mode feature) — Leicht now reaches the scoring cone at ~1.5s and full capture at ~5.1s vs. Standard's ~1.5s/~4.0s, while Schwer reaches the cone but never fully captures within 40s — then confirmed by a second human playtest ("fühlt sich alles so an wie es soll"). Known, deliberately deferred (parked as Minor, not load-bearing): the fix wave's restored pause-badge outline stroke is the same color as its own fill and thus visually a no-op; `R`/`D` duplicate the same 6-line round-reset block verbatim (same shape as AP2's `auto_time`/`manual_time` reset omission — recommended extracting a helper, not done); hint text is frame-counted, not time-based, and isn't cleared across a reset; no zero-division guard if a future difficulty level ever used a 0° tolerance zone (unreachable with the current 3 levels); the mutual-exclusion invariant — this branch's central correctness claim — has no automated regression test since it lives entirely in the untested Pygame event loop (recommended extracting `auto_toggle_allowed(difficulty)`/`difficulty_change_allowed(auto_mode)` as pure, testable predicates in a future hardening pass).
- **2026-08-21**: Fix (post-AP4) — closed a naive-swing scoring exploit on `Schwer`. User observation: just swinging (no attempt to balance) on `Schwer` could outscore the same behavior on `Leicht`, inverting the intended difficulty/skill relationship. Root-caused via simulation (`compute_score_increment`'s outer scoring cone, `max_angle`, was hardcoded to 90° for every difficulty, unlike `bonus_zone`/`tight_bonus_zone` which already scale per level): a fixed-period bang-bang "naive swing" resonates with `Schwer`'s heavier/less-damped pendulum, and at one arbitrary shared period scored 228 vs. `Leicht`'s 35 — but a fuller sweep (each difficulty at its *own* best resonance period) showed the real gap was 228 vs. 233, roughly tied, because the wide 90° cone rewards merely sweeping through it regardless of skill. Fix: `compute_score_increment()` gained a `max_angle` parameter (default unchanged at 90°, every existing caller unaffected); `DIFFICULTY_LEVELS["Schwer"]["max_angle_deg"] = 40.0` (`Leicht`/`Standard` stay at 90.0). Verified end-to-end against the real FMU: best naive-swing score is now `Leicht` 233 > `Standard` 125 > `Schwer` 111 — intended ordering restored. Legitimate precise play is unaffected since `Schwer`'s `bonus_zone`/`tight_bonus_zone` (8°/3°) sit far inside the new 40° cone. Two new regression tests (`tests/test_scoring.py::test_custom_max_angle_zeroes_score_beyond_it`, `tests/test_difficulty.py::test_schwer_max_angle_narrower_than_leicht_and_standard`); all 65 tests pass.

## Conventions (binding across all APs)

- **Winkelkonvention**: `phi = 0` is the stable, hanging rest position; `phi = math.pi` is the unstable, upright target position. Any new controller, scoring logic, or difficulty mechanic must respect this — it is not derivable from variable names alone. See `protokoll.md` §2 for the Lagrange derivation this is based on.
- **FMU solver**: export with the **Euler** solver, not CVODE. CVODE FMUs reproducibly crash (`STATUS_HEAP_CORRUPTION`, exit `-1073740940`) under Windows + `fmpy`, even though the model itself is fine — see `AP1_Validierung.md` §3.3. This is a Windows/fmpy interaction, not a modeling error; don't waste time re-diagnosing it, just use Euler.
- **Validating a new/changed model against the reference**: don't compare time-domain trajectories — the system is chaotic (unstable, nonlinear) and trajectories diverge exponentially regardless of model correctness. Compare instantaneous accelerations at fixed states instead (`a`/`alpha` vs. `prismatic.a`/`revolute.a`), which is algebraic and insensitive to the chaos problem. See `AP1_Validierung.md` §3.1–3.2.
- **Controller interface**: new controllers subclass the existing `Controller` base class in `pendulum_game_controlled.py` (see `SimpleController` for the reference implementation, a PD controller on `phi`/`vphi`). This is AP3's contract — don't modify the base class or `SimpleController` outside AP3.
- **Reset uses `fmu.reset()` + `setupExperiment()` + `enterInitializationMode()`/`exitInitializationMode()`**, never tearing down and recreating the FMU instance. The `setupExperiment()` call after `reset()` is required — `fmpy` does not raise on a rejected FMI2 state transition, so omitting it fails silently rather than loudly.
- **`leaderboard.csv` compatibility**: the real file accumulates real rows over time and will not always match whatever schema the current code assumes. Any code that reads it must tolerate rows written by older code (missing columns entirely, not just missing values) — see `_ensure_leaderboard_columns()` in `pendulum_game_controlled.py` for the pattern.
- **Testing**: `pytest`, tests in `tests/`. Pure functions (scoring, mode classification, leaderboard I/O) are unit-tested directly. Anything requiring a live Pygame `Surface` (rendering, the interactive event loop) has no automated test — it needs a human running the game with a real display. Flag this explicitly rather than silently skipping verification when a task touches `redraw()` or the event loop. FMU-driven tests that don't touch Pygame (e.g. `tests/test_numerical_stability.py`) are fine and precedented — guard them with `pytest.mark.skipif(not os.path.exists(fmu_path), ...)` so they skip gracefully in a worktree without the (gitignored) `.fmu` copied in, rather than erroring.
- **FMU co-simulation must be sub-stepped, not called with the full `dt` in one `doStep()`**: `run_game()` calls `fmu.doStep()` `SUBSTEPS=10` times per game frame (`inner_dt = dt/SUBSTEPS`), holding `tau` constant across the substeps. This is required, not an optimization — explicit-Euler integration of the FMU at the full `dt=0.02s` step numerically injects energy into the lightly-damped pendulum (confirmed via direct energy-conservation analysis and against the real FMU, AP3 Teil 2), causing free motion to diverge instead of decay. Any new code that calls `fmu.doStep()` must preserve this pattern.
- **Timing constants**: `dt = 0.02` (50 Hz simulation step), `GAME_DURATION = 40` seconds per round, `MAX_TAU = 10.0` (force is bang-bang, `tau = ±MAX_TAU` via arrow keys — no finer force dosing until a difficulty AP explicitly adds it).
- **Setting an FMU's initial state from outside**: use the model's `phi0`/`vphi0` parameters (`causality=parameter, initial=exact`, settable via `fmu.setReal()` before `enterInitializationMode()`), never the `phi`/`vphi` output variables (`causality=output, initial=calculated`) — those are solver-internal and silently ignore external `setReal()` calls, both before and during simulation (confirmed empirically against `InvertedPendulumMB.fmu`, AP3 Teil 3). This also means no FMU state is settable mid-simulation via `setReal()` on `phi`/`vphi`/`s`/`v` — any runtime disturbance must go through the genuine `tau` input instead. See `benchmark_controllers.py`'s `simulate_run()` for the working pattern. The model's mass/friction parameters — `m_cart`, `m_pend`, `d_cart`, `d_pend` — are the same `causality=parameter` family and follow the identical rule (settable pre-init, never mid-simulation); see `apply_difficulty_physics()`/`reset_round()` in `pendulum_game_controlled.py` (AP4) for the pattern.
- **Difficulty/Auto mutual exclusion (AP4)**: Manual-mode difficulty selection (`D` key) and Auto mode (`H` key, the AP3 controllers) are mutually exclusive by construction — `H` is a no-op unless `difficulty == "Standard"`, and `D` is a no-op while `auto_mode` is `True`. This guarantees Auto mode never runs against difficulty-altered mass/friction, so LQR/SwingUp never need retuning per difficulty. Any future change touching either key's handler must preserve this invariant — it currently has no automated regression test (lives in the untested Pygame event loop), so a reviewer must re-derive it by hand from the code, the way AP4's final review did.

## Ideas / Backlog (not yet started)

- **CVODE-Solver-Vergleich via WSL**: ✅ **Spike abgeschlossen (2026-08-21)**. CVODE-Export lief unter WSL2/Ubuntu (OpenModelica 1.27.0, `export_mb_cvode.mos`) sauber durch — kein Absturz, bestätigt die Windows+fmpy-`STATUS_HEAP_CORRUPTION` als reine Plattform-Eigenheit, kein Modellierungsfehler (siehe Convention "FMU solver"). Vergleich der resultierenden Linux-CVODE-FMU gegen die dokumentierten Euler-Werte (`AP3_Reglervergleich.md`, via unverändertem `benchmark_controllers.py` in einem WSL-venv): PD-Einzugsbereich, LQR-Einzugsbereich (10°), Reaktionszeit (0.00s/0.70s) und Robustheit (Peak-Auslenkung 1.02°) sind **identisch** zwischen Euler und CVODE — kein Reglerretuning nötig. Einziger Unterschied: die SwingUp-Fangzeit ab der realen Spiel-Startbedingung steigt von 4.20s (Euler) auf 5.84s (CVODE), +39 %. Plausible Ursache: der `SUBSTEPS=10`-Mechanismus (siehe Convention "FMU co-simulation must be sub-stepped") kompensiert eine numerische Energie-Einspeisung, die spezifisch aus expliziter Euler-Integration bei `dt=0.02s` stammt (`AP1_Validierung.md` §6.7); unter CVODEs genauerer adaptiver Integration entfällt dieser Effekt, wodurch der energiebasierte SwingUp-Regler länger für dieselbe physikalische Energiemenge braucht — keine Modellierungs- oder Regler-Schwäche, sondern eine reale Solver-Genauigkeits-Konsequenz. `export_mb_cvode.mos` bleibt im Repo (reproduzierbar via `omc export_mb_cvode.mos` unter WSL); die exportierte `InvertedPendulumMB_cvode_linux.fmu` selbst ist wie alle FMUs gitignored. Relevant als Evidenz für AP5.
  **Follow-up CVODE-Retuning (2026-08-21)**: `SwingUpController` bekam optionale Konstruktorparameter (`k_energy`, `capture_theta`, `capture_vphi`, `release_theta`; alle `None`-Default = unverändertes Euler-Verhalten für jeden bestehenden Aufrufer inkl. `benchmark_controllers.py`); `run_game()` liest FMU-Pfad und Tuning optional aus `PENDULUM_FMU`/`PENDULUM_SWINGUP_K_ENERGY`/`PENDULUM_SWINGUP_CAPTURE_THETA_DEG` (alle standardmäßig = heutiges Windows/Euler-Verhalten, byte-identisch ohne gesetzte Variablen). Motivation: WSLg läuft auf dem Entwicklungssystem (`DISPLAY=:0`, Weston aktiv trotz Windows 10 Build 19045), das Spiel ist unter WSL also tatsächlich interaktiv spielbar, nicht nur headless benchmarkbar. Vor dem Retuning wichtige Klarstellung geprüft: der LQR-Gain (analytisch via `solve_continuous_are`) und die Energie-Pump-Formel selbst sind physikalisch hergeleitet und lösen-unabhängig; `d_pend=0.01` ist ein Modellparameter, identisch in beiden FMUs (beide aus derselben `.mo`-Datei exportiert). Nur die *numerischen Tuning-Konstanten* (`K_ENERGY`, Hysterese-Schwellen) wurden historisch ausschließlich gegen die Euler-FMU interaktiv verifiziert — der CVODE-Vergleich ist also für den Regelgesetz-Kern fair, für die konkreten Zahlenwerte nicht automatisch. Zwei Sweeps gegen die reale CVODE-FMU: (1) `K_ENERGY` (10 bis 50) zeigt nur marginale, schnell sättigende Verbesserung (5.84s → 5.54s ab K_ENERGY≈40, danach flach) — Ursache ist Aktuator-Sättigung (`tau` klemmt bei `MAX_TAU=10.0` unabhängig von `K_ENERGY`), nicht ein suboptimaler Tuning-Wert. (2) Hysterese-Schwellen (`CAPTURE_THETA` 5°/10°/15°/20° × `CAPTURE_VPHI` 1.0/2.0/3.0, bei festem `K_ENERGY=25.0`) zeigen einen spürbareren Effekt: `CAPTURE_VPHI` ist in diesem Bereich wirkungslos (theta ist die bindende Bedingung), aber `CAPTURE_THETA` ist nicht monoton — zu weit (20°) verschlechtert deutlich (6.56s), weil die LQR-Übernahme zu früh passiert, bevor genug Energie gepumpt ist, und über die `RELEASE_THETA`-Hysterese wieder in Swing-up zurückfällt (ein "Fehlstart"); enger als der Euler-Default (5° statt 10°) ist leicht besser. Bestes gefundenes Setup: `K_ENERGY=25.0` + `CAPTURE_THETA=5°` → 5.48s (nur ~6% besser als ungetuned 5.84s). Die Lücke zu Eulers 4.20s bleibt strukturell unschließbar durch Retuning (Eulers numerische Energie-Einspeisung wirkt wie ein Bonus oberhalb des physikalischen Kraftlimits). Übernommen als dokumentierte, nicht-default CVODE-Werte — Nutzung: `PENDULUM_FMU=InvertedPendulumMB_cvode_linux.fmu PENDULUM_SWINGUP_K_ENERGY=25.0 PENDULUM_SWINGUP_CAPTURE_THETA_DEG=5.0 python pendulum_game_controlled.py` unter WSL.
- **SwingUp-Regler: kein Fangen nach Überschwingen** (Nutzerfund beim AP4-Playtest, 2026-08-19): wenn das Pendel beim Hochschwingen über die aufrechte Lage hinausschießt (Überschwinger), kann `SwingUpController` es danach nicht mehr stabilisieren. Vermutlich ein Grenzfall der Hysterese-Umschaltung (`CAPTURE_THETA=10°`/`CAPTURE_VPHI=2.0` → `RELEASE_THETA=25°`, siehe AP3 Teil 2): bei hoher Winkelgeschwindigkeit im Moment des Überschwingens könnte das Capture-Fenster verpasst oder die LQR-Übernahme aus ihrem gültigen linearen Bereich heraus getriggert werden — noch nicht analysiert. Betrifft nur den Auto-Modus (AP3), nicht AP4. Noch nicht begonnen; bräuchte eigenen Brainstorming/Plan-Zyklus.

## Key Files

- `pendulum_game_controlled.py` — the game (Pygame + fmpy + pandas + numpy/scipy since AP3), all game logic lives here (flat-function style, no submodules).
- `requirements.txt` — pinned dependency versions (added in AP3; keep in sync with what's actually installed/verified working).
- `*.mo` / `export.mos` / `export_mb.mos` — OpenModelica models and FMU export scripts. `.fmu`/`.log`/build artifacts are gitignored; copy them manually into any worktree that needs to run the game.
- `leaderboard.csv` — persistent leaderboard, real user data, schema evolves (see convention above).
- `protokoll.md` — Lagrange derivation, linearization, controller theory. Reference for the written report (AP5), don't re-derive.
- `AP1_Validierung.md` — AP1's model validation writeup.
- `AP2_Validierung.md` — AP2's implementation validation writeup (tests, review process, bugs found during development).
- `benchmark_controllers.py` — AP3 Teil 3's headless controller-comparison benchmark (no Pygame), drives the three controllers against the real FMU and writes `AP3_Reglervergleich.md` + `benchmark_plots/`. Run with `python benchmark_controllers.py` (needs the gitignored FMU present).
- `AP3_Reglervergleich.md` — AP3 Teil 3's generated controller-comparison report (stability/reaction-time/robustness), source material for AP5 Kapitel 5.
- `Projektanweisung_Invertiertes_Pendel.md` — full project brief, all APs, milestones, deliverables.
- `docs/superpowers/plans/` — implementation plans for individual APs (subagent-driven-development / executing-plans format).
